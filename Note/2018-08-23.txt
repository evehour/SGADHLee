18일차
가제: 게임 UX/UI 기획하기 (아이템 사용)

1교시



2교시
조사::사원수
w - 회전 비율  값

조사 :: &와 *의 const 위치에 대한 정의

Culling - VS처리 직전에 계산됨. (버텍스대상(면)으로 안그림)
Cliping - PS처리 직전에 계산됨. (픽셀대상으로 안그림)

Cliping에 Clip(x): x가 0이하 일경우, 해당 픽셀을 그리지 않음.
Clip(Alpha - 0.1)로 함.

clip함수는 값이 0이하가 안되면 코드를 아래로 넘기지 않는다.
alpha to coverage 라는 state가 있음.


3교시

1. 지형 클릭한 곳에 나무 이미지 박기. 이동/크기조절 다 되야 함.
2. 배치하고 45도를 틀고, 또다른걸 박아서 십자형태로 만들기.
3. Debug Draw 영역을 선으로 보여주는 것.
4. redo / undo -> Command pattern을 사용해서 만들기.

Command pattern
스택을 두개를 만들어야 함.
행동을 하게되면 스택을 넘김.

