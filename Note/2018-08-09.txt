2018-08-09
가제: 게임 UI/UX 분석하기 (인벤토리)

2교시
Material에는 Mesh와 MeshPart가 있다.
Mesh 안에서도 여러가지 Mesh가 존재할 수 있는데, 이것들을 MehsPart로 정의한다.

Mesh의 vertexInPolygon은 오른손 좌표계이기 때문에 왼손좌표계로 바꿔주기 위해서 for문을 --로 돌림. ++이 아니라!

CPU 안에 decorder가 있다.
decorder가 명령을 수행하는데 1:1로 수행되면 저수준 언어.
때문에 c는 저수준언어고 cpp는 고수준언어다.
ex)
int a = 0; // c는 이 문법이 불가능함. 값 대입은 밑에줄에 넣어야 한다고 함.

벡터연산에서 크기는 의미가 없으므로 방향으로만 하기 위해 노멀라이즈화 함.

Material은 하나만 쓰는게 아님. 각 레이어별로 Material이 있는데 게임업계에서는 레이어를 무조건 가장 마지막에있는 것(0번 인덱스)만 씀.


3교시
FBX는 UV의 v의 값이 밑으로 내려가는게 증가하는 것이기 때문에 반전시켜줘야 함.

